fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
geoalchemy2==0.15.1
shapely==2.0.6
psycopg2-binary==2.9.9
alembic==1.13.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.4
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
faker==25.8.0
pytest-cov==5.0.0
pyotp==2.9.0

# Face-similarity scoring (sighting <-> case photo) -- see
# app/services/face_match_service.py for why classical CV (Haar cascade +
# LBP histograms) was used instead of a deep embedding model.
opencv-contrib-python-headless==4.10.0.84
scikit-image==0.26.0

# Missing-person flyer PDF generation (app/services/flyer_service.py)
reportlab==5.0.0
qrcode[pil]==8.2

# SMS-based two-factor auth (app/core/sms.py) -- only imported when
# SMS_BACKEND=twilio; the default "console" backend needs no SDK.
twilio==9.10.9
