[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # long lines flagged individually rather than hard-failing CI
